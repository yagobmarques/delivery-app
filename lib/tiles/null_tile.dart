import 'package:flutter/material.dart';

class NullTile extends StatelessWidget {
  const NullTile({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}